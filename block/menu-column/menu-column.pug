mixin b-menu-column(data, cls, curUrl)
	if data
		+b.MENU.menu-column(class= cls + " list-group")
			+e.UL.list
				each e in data
					- var active= curUrl == e.link ? 'active' : ''
					+e.LI.list-item(class="list-group-item list-group-item-action")
						+e.A.list-item-link(href= e.link)= e.name